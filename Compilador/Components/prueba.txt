using Data;

class AllProds 
{
  int nPoints;
  char exclaim;
  char caracterEspecial;
  int[] arreglo;
  
   class Point {
    int x, y, x, m; // aqui hay un error de declaracion de variables
    char exclaim; // aqui hay un error de declaracion de variables
  }

  char[] msg;
  boolean test1;
  Point point;

  void WriteError (int julio)
  {
    char[] msg; //error
    Point point;        // aqui hay un error de declaracion de variables dentro de un metodo
    point = new Point;
    char exclaim;       // aqui hay un error de declaracion de variables dentro de un metodo
    point.j = 0;
    add(arreglo, 1);
    del(arreglo, 1);
    len();              // aqui hay un error de llamada a metodo
    len(arreglo);
    int x;
    int b;
    int b;             // aqui hay un error de declaracion de variables
    char exc;
    exc = (char) point.x;
    boolean test1;
    exclaim = (char) point.x;
    caracterEspecial = 'a';
    test1 = true;

    while(b<5){
        write(msg[b]);
        b++; 
        int cesar;
	 if(exc == 'a'){
		len(arreglo);
	 }
		
    }
 
    for(b;b<7;b++;){
        write(msg[b]);
    }

    write(exclaim);

    while (b >8) {
        b--;
        if (b < 3 && test1==1)  // aqui hay un error de tipos de datos
            break;
        write(msg[b], 2);
    }
    
  }

  int GetX (Point y, int x, char ju) 
  { 
        return y.y; 
  }

  int GetX ()   // aqui hay un error de declaracion de metodo
  { 
     int k;
  }
  void SetX (int m, Point p2) {
        int[] arreglito;
        if (m > -10 && m < 10) {
          int camino; 
          camino = GetX(p2, 2, 'a');  // aqui hay un error de llamada a metodo
        }
        else {
             WriteError(m);
        }
        m = 43/4%4+12+m;
        return;
  }

  void SetMsg () {
    msg = new char[5];  
    msg[1] = 'E';
    msg[2] = 1;  // aqui hay un error de tipos de datos 
    read(msg[3]);
  }

  void SetPoints ()
  {
    int[] pArr;
    int c;

    while (c <5) {
      c++;
    }
  }

  int prueba(Point x, int w)
  {
      return x.x;
    
  }

  void Main ()
  {
    Point x3;
    int a;
    SetMsg();
    SetPoints();
    read(a);
    if (a < 0){
      Point x3;  // Variable ya existe dentro del scope
      char caracterEspecial;
      write(GetX(x3, 2, 'a'));
    }
    else
      WriteError(1);
    write('n');

  }
  
   class Point {  // aqui hay un error de declaracion de clase
    int x, y, x, m;
    char exclaim;
  }
}

